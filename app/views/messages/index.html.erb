<% @messages.each do |message| %>
  <div class="col-md-10 col-md-offset-1">
    <div class="message"
      <h2 class="message-title"><%= message.title %></h2>
      </br>
      <%= link_to "Join Conversation", message %>
    </div>
  </div>

<% end %>
